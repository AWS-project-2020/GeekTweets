<% if(posts.length > 0){ %>
<div class="container">
    <% posts.forEach(function(post, index){ %>
    <% if(index % 1 === 0){ %>
    <div class="row">
        <% } %>
        <div class="col-md-4">
            <h2><a href="/u/<%= post.user %>">
                    <%= post.user %>
                </a></h2>

            
            <p>
                <%= post.post %>
            </p>

            <p>
                <small>
                      Ecrit à <%= post.time.toLocaleString('en-GB')%>
                </small>
            </p>
        </div>
        <% if(index % 1 === 0){ %>
    </div>
    <!-- end row -->
    <% } %>
    <% }) %>
   
</div>
<% } %>
